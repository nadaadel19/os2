on:
  push:
    branches:
      - master


jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    
      - uses: actions/checkout@v3  
      - name: "docker login"
        run: docker login --username nadaadel19 --password dckr_pat_fKTm94hZXuwy58LgEXRMlqa3F5g
  
